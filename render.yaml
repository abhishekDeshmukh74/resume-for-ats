services:
  # ── Backend (FastAPI) ──────────────────────────────────
  - type: web
    name: resume-for-ats-api
    runtime: docker
    region: oregon
    plan: free
    rootDir: backend
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.11"
      - key: GROQ_API_KEYS
        sync: false          # comma-separated Groq API keys
      - key: GROQ_MODEL
        value: llama-3.3-70b-versatile
      - key: MONGODB_URL
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: GEMINI_MODEL
        value: gemini-2.0-flash
      - key: LLM_PROVIDER
        value: groq
      - key: ALLOWED_ORIGINS
        sync: false          # set to your frontend URL after deploy

  # ── Frontend (React / Vite) ────────────────────────────
  - type: web
    name: resume-for-ats
    runtime: static
    rootDir: frontend
    buildCommand: npm install --legacy-peer-deps && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /api/*
        destination: https://resume-for-ats-api.onrender.com/api/*
    envVars:
      - key: NODE_VERSION
        value: "20"
